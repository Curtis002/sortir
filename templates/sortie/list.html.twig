{% extends 'base.html.twig' %}

{% block title %}  Sorties {% endblock %}

{% block body %}
    <div class="container">
{#        <div class="row">#}
{#            {% include 'sortie/filter.html.twig' with {form: form} only %}#}
{#        </div>#}
        <div>

            <table class="table">
                <thead>
                <tr>
                    <th scope="col">Nom de la sortie</th>
                    <th scope="col">Date de la sortie</th>
                    <th scope="col">Clôture</th>
                    <th scope="col">Inscrits / Places</th>
                    <th scope="col">Etat</th>
                    <th scope="col">Inscrit</th>
                    <th scope="col">Organisateur</th>
                    <th scope="col">Actions</th>
                </tr>
                </thead>
                <tbody>
                    {% for sortie in sorties %}
                    <tr>
                        <td>{{ sortie.nom }}</td>
                        <td>{{ sortie.dateHeureDebut|format_datetime('short', 'none', locale='en') }}</td>
                        <td>{{ sortie.dateHeureDebut|format_datetime('short', 'none', locale='en') }}</td>
                        <td>{{ sortie.nbInscriptionsMax }}</td>
                    {#   <td>{{ sortie.etatSortie}}</td> #}
                        <td>TODO</td>

                        <td><a href="{{ path('sortie_detail', {'id': sortie.id}) }}">Afficher</a></td>
                        {# {{ sortie.organisateur.pseudo }}</td> #}<td>
                        <td><a href="{{ path('sortie_join', {'id' : sortie.id}) }}">S'inscrire</a></td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>

        </div>
    </div>

{% endblock %}