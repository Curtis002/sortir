{{ form_start(sortiesForm) }}
<div class="container">
    <div class="row">
        <div class="col-sm">
            <h4>Campus</h4>
            {{ form_row(sortiesForm.campus) }}

            <h4>Le nom de la sortie contient</h4>
            {{ form_row(sortiesForm.q) }}

            <h4>Entre {{ form_row(sortiesForm.dateDebut) }} et {{ form_row(sortiesForm.dateFin) }} </h4>
        </div>
        <div class="row">
            <button type="submit" class="btn btn-primary">Rechercher</button>
        </div>
    </div>
</div>
{{ form_end(sortiesForm) }}

